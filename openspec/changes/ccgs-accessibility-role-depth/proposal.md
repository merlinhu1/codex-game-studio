## Why

`accessibility-specialist` is the first direct CCGS role-package depth gap in the generated remaining-gap report. Closing it proves the new generated gap report can drive one concrete parity reduction before broader workflow/template work.

## What Changes

- Upgrade the `accessibility-specialist` Codex role package with CCGS-depth responsibilities, inputs, output schema, quality gates, collaboration notes, stop conditions, and handoff contract.
- Mark `accessibility-specialist` as a CCGS parity-upgraded role so the parity matrix reports the direct role row as implemented.
- Add role-package and parity-audit regression tests for the new contract and remaining-gap count reduction.
- Regenerate CCGS parity references after implementation.

## Capabilities

### New Capabilities

- `ccgs-accessibility-role-depth`: CCGS-depth accessibility role contract and parity reporting.

### Modified Capabilities

None.

## Impact

- `src/roles.ts`
- `tests/roles.test.ts`
- `tests/ccgs-parity-audit.test.ts`
- `references/ccgs-surface-parity-matrix.json`
- `references/ccgs-remaining-gap-tasks.md`
